{
  "name": "dobairro",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "packageManager": "pnpm@9.1.1",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "workspaces": [
    "config/*",
    "packages/*",
    "apps/*"
  ]
}
